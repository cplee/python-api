machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t cplee/python-api .

test:
  override:
    - docker run -d -p 8000:8000 cplee/python-api; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:8000